[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kaula-core"
version = "0.1.0"
description = "Kaula contracts: domain types, seam Protocols, and the self-healing loop state machine"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/kaula"]
